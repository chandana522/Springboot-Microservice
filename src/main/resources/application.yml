server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/chandana522/config-server.git
          clone-on-start: true
    loadbalancer:
      ribbon:
        enabled: false